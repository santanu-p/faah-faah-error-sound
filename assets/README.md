# Assets

Put your sound files in this folder if you want to bundle them with the extension.

- Optional bundled default file name: `default.mp3`

The extension now primarily looks in `audio/` (recursively) and randomly picks one MP3.

You can also point to any absolute MP3 file or folder using extension settings.
